# OpenClaw Gateway Configuration
# Copy this file to .env and update with your values

# Gateway Authentication Token
# Generate a secure random token (e.g., using: openssl rand -hex 32)
# This token is required when gateway.auth is set to "token"
# You can also pass --token flag to the gateway command instead
OPENCLAW_GATEWAY_TOKEN=your-secure-token-here

# Optional: Skip service check on startup
# OPENCLAW_SKIP_SERVICE_CHECK=true

# Optional: Node environment
# NODE_ENV=production

